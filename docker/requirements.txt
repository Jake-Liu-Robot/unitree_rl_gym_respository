# ── Core scientific / RL stack ────────────────────────────────
scipy
matplotlib
tensorboard

# ── Robotics / control ───────────────────────────────────────
# numpy is pinned and force-reinstalled in the Dockerfile after Isaac Gym.
# gym/gymnasium: only one needed; rsl_rl works with gym 0.26.x but
# mujoco 3.x ships its own bindings — no separate gymnasium needed.
gym==0.26.2            # OpenAI Gym (rsl_rl dependency)

# ── Utilities ────────────────────────────────────────────────
pyyaml
tqdm
Pillow

# ── MuJoCo extras ────────────────────────────────────────────
dm-control>=1.0.14          # DeepMind control suite (uses MuJoCo backend)
imageio
imageio-ffmpeg
